# Blockchain Configuration
# RPC URL for the blockchain network (e.g., Ethereum mainnet, testnet, or local)
RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# Private key of the wallet that has permission to mint tokens
# This should be the private key of an account with minting privileges on the contract
PRIVATE_KEY=your_private_key_here

# Note: Make sure to add .env.local to your .gitignore file to keep these secrets safe
